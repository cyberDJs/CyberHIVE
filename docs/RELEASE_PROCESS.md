# Release Process — Seed

Every release should be reproducible, versioned, accompanied by changelog/compatibility notes and have an explicit rollback path. Production auto-update is not an MVP requirement; safe staged update is.
