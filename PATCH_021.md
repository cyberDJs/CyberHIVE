# Patch 021 — Node Worker Runtime Loop MVP

Adds `cyberhive_core.worker_runtime` with signed ACTION envelope processing, ACK emission, handler dispatch, ACTION_RESULT emission, and worker outcome records.
